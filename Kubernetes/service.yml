apiVersion: v1
kind: Service
metadata:
  name: nsflix-app
  labels:
    app: nsflix-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: nsflix-app
